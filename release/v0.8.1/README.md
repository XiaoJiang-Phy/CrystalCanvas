# v0.8.1 release candidate

This patch adds an optional structurally equivalent top/bottom slab mode, repairs slab layer-buffer bounds and preserves source-site metadata through supercell/slab construction. Miller indices use the current input cell; valid P1 inputs are accepted. Normal-repeat and added-height vacuum conventions remain unchanged. The legacy termination command is periodic coordinate repositioning, not bulk termination reconstruction.

Feature baseline: `eaaaa847e6830d567995896432fa823b13c53983`.

## Completed software checks

- Rust format and check: PASS. Full `cargo test --offline --all-targets --no-fail-fast`: 329 passed, 0 failed, 5 ignored. Desktop-dependent tests and unavailable benchmark measurements are not counted as platform validation.
- C++ CTest: all 6 suites PASS, including source-site multiplicity, oblique-cell geometry, coordinate reconstruction and layer-buffer capacity.
- Layer operations with AddressSanitizer/UBSan: 15 tests PASS after compiling the test framework and kernel consistently. The initial mixed cached-library invocation failed during test registration and is not counted as PASS.
- Frozen frontend dependency install, IPC inventory/contract, 38 IPC tests, 12 field/release tests, TypeScript and frontend build: PASS.
- Prior ordinary-mode candidate Intel/Metal hidden-window SYNC smoke: PASS, process exit 0. This covers state mutation, slab/supercell and undo integration, not a complete visual review.
- Native x86_64 debug app bundle: built successfully with the symmetric mode for reviewer testing, bundle version 0.8.1.
- New version-bound PNG and GLB artifact triplets: generated by existing explicit software fixture tests; hashes checked by RELEASE-2. These are software-contract fixtures, not scientific figures or visual-quality evidence.

## Explicitly limited Blender waiver

The user authorized waiving Blender headless and GUI validation for **v0.8.1 only**. The evidence records `WAIVED`, never `PASS`. The attempted Blender 4.4 sandbox run crashed during GPU initialization, and the normal-permission retry was not executed after an approval-service rejection. Neither result establishes Blender compatibility. The waiver does not apply to later releases or native platform validation.

## Remaining closure

`release-evidence.json` remains `RELEASE_CANDIDATE` pending current-candidate native visual review and platform-validation closure. Historical v0.8.0 attestations are not reused. No release tag is pushed until publish-mode RELEASE-2 passes.

The bounded software-only exception and Blender waiver are recorded in `EVOLUTION.log`. Software regressions do not certify material-specific surfaces or physical correctness.

## Symmetric-mode regression

`tests/data/rutile.cif` → 2×2×2 → (110), 3 repeats, 18 Å added vacuum: 144 sites (48 Ti, 96 O), three replicas per source site, preserved occupancy, centered envelope and independently matched finite inversion. All 22 slab Rust tests and all 6 C++ suites pass, including mirror-only symmetry and rejection of periodic-only equivalence. IPC 38 tests and frontend build pass. No further physical or Blender gate is introduced.
